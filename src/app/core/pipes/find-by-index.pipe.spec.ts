import { FindByIndexPipe } from './find-by-index.pipe';

describe('FindByIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new FindByIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
